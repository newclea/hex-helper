# GameBuddy Windows 实机验收

代码及自动化验证完成后，在实际运行英雄联盟的 Windows 电脑上执行本清单。

当前多显示器拖动、Windows x64 断网首次安装、MeloTTS 真实音频播放和
进程清理均待 Windows 实机验收，不能以 macOS 自动化或 mock 测试替代。

2026-09-22 文档与自动化验证在 macOS 主机上执行。本轮新增的 Windows 断网安装、
真实声音、音频设备和进程检查记录均标记为“未执行”。

## 环境记录

- Windows 版本：未执行
- 显示缩放比例：
- 显示器数量与排列：
- 网络状态：未执行
- 音频设备：未执行
- GameBuddy 日志路径：未执行
- 验收人：未执行
- 验收时间：未执行

## 检查项

### 1. 正常启动

- 准备：在未安装 sherpa-onnx 的 Windows x64 电脑安装 Python 3.11 和
  Visual C++ 2015–2022 x64 运行库，启动前禁用网络。
- 操作：双击根目录的 `一键启动小猫.cmd`。
- 预期：资源校验通过，pip 只使用项目内置 wheelhouse 且带 `--no-index`；
  GameBuddy 正常启动并显示小猫，不修改 PowerShell 全局执行策略。
- 实际：未执行（当前主机为 macOS）
- 结果：未执行

### 2. 右键菜单范围

- 准备：启动 GameBuddy，确认小猫显示。
- 操作：分别右键小猫、气泡、按钮外透明区域。
- 预期：只有右键小猫时出现菜单，菜单依次包含“关闭语音”和“退出”。
- 预期：关闭语音后再次打开菜单，首项动态变为“开启语音”。
- 实际：
- 结果：通过 / 不通过

### 3. 菜单收起与退出

- 准备：右键小猫打开菜单。
- 操作：先点击菜单外确认收起；再次打开后点击“退出”。
- 预期：点击外部只收起菜单。
- 预期：点击“退出”后 Overlay、轮询、视觉和语音子进程全部结束。
- 预期：退出时不显示二次确认。
- 预期：语音播报不启动 PowerShell worker；退出后任务管理器中没有遗留
  本应用启动的 Python 离线语音 worker。
- 实际：
- 结果：通过 / 不通过

### 4. 短按重试兼容

- 准备：进入允许手动重试识别的状态。
- 操作：左键短按小猫并在 500ms 内松开。
- 预期：窗口不移动，仍触发现有重新识别逻辑。
- 实际：
- 结果：通过 / 不通过

### 5. 500ms 长按拖动

- 准备：记录 Overlay 初始位置。
- 操作：分别按住不足 500ms、按住达到 500ms 后移动鼠标。
- 预期：不足 500ms 不拖动；达到 500ms 后小猫跟随鼠标平滑移动。
- 实际：
- 结果：通过 / 不通过

### 6. 多显示器绝对坐标

- 准备：至少使用两块显示器，其中一块位于主屏左侧或上方，并保留任务栏。
- 操作：在各显示器之间来回拖动小猫，并拖到每块屏幕的边界。
- 预期：跨屏连续，负坐标不镜像；小猫不跳到另一侧，也没有固定分辨率上限。
- 实际：
- 结果：通过 / 不通过

### 7. 四边与四角气泡

- 准备：显示一条能在当前显示器内完整展示的推荐气泡。
- 操作：依次把小猫拖到上、下、左、右边界及四个角落。
- 预期：气泡位于相反方向或斜对角，内容一次完整显示，不滚动、不压缩字号。
- 实际：
- 结果：通过 / 不通过

### 8. 小猫位置锚定

- 准备：记录小猫的屏幕位置。
- 操作：不移动小猫，依次显示短文本和长文本气泡，再完成一次拖动。
- 预期：气泡大小或方向变化时小猫不漂移；拖动落点与鼠标一致。
- 实际：
- 结果：通过 / 不通过

### 9. 自适应气泡宽度与换行

- 准备：分别准备短文本和包含长玩法名、标点、多段说明的长文本。
- 操作：先显示短文本，再显示长文本，检查气泡宽度和每行文字。
- 预期：短文本气泡按内容收缩；长文本最大有效宽度约 780px，自然换行且全部显示。
- 预期：没有仅剩 1～2 个字符的末行，没有结束标点出现在行首，文字不覆盖按钮。
- 实际：
- 结果：通过 / 不通过

### 10. 标题和内容字重

- 准备：显示正常推荐结果。
- 操作：检查“当前推荐”“当前玩法”“所需海克斯”“所需装备”四行。
- 预期：四个标题使用普通字重，标题后的值使用粗体；换行后仍保持正确字重。
- 实际：
- 结果：通过 / 不通过

### 11. 五种参考姿态

- 准备：依次触发等待、应用刚启动、推荐结果、未识别和 OCR 识别。
- 操作：对照已批准的五张参考图检查小猫姿态。
- 预期：依次为等待、挥手、推荐指向、失败和抓头；没有姓名文字或 Codex 宠物。
- 实际：
- 结果：通过 / 不通过

### 12. 眨眼与状态动作

- 准备：依次进入各个动画状态。
- 操作：每个状态至少观察两个循环。
- 预期：所有状态约每 3 秒自然眨眼一次；启动挥手时身体稳定。
- 预期：推荐指向、失败和抓头动作与参考一致，切换及时且循环连续。
- 实际：
- 结果：通过 / 不通过

### 13. OCR 与长按拖动抓头

- 准备：依次进入 OCR 识别和可拖动状态。
- 操作：观察 OCR 动画；再长按 500ms 并持续拖动两秒后松开。
- 预期：OCR 和拖动期间都显示连续的轻微抓头动作；松开后恢复当前业务动画。
- 实际：
- 结果：通过 / 不通过

### 14. 动画静态回退

- 准备：退出程序，临时将 `assets/gamebuddy/animations.json` 改名，验收后恢复原名。
- 操作：重新启动并完成一次识别与推荐。
- 预期：程序使用原静态小猫正常启动，识别、推荐、短按和退出功能可用。
- 预期：日志记录动画资源错误。
- 实际：
- 结果：通过 / 不通过

### 15. 默认语音与开关持久化

- 准备：删除或备份 `%LOCALAPPDATA%\LoLRecognitionOverlay\overlay.json` 后启动 GameBuddy。
- 操作：确认菜单显示“关闭语音”；关闭语音，退出并重新启动，再重新开启语音。
- 预期：首次启动默认开启；关闭后菜单变为“开启语音”，重启后仍关闭。
- 预期：重新开启后可正常播报。
- 实际：
- 结果：通过 / 不通过

### 16. 配置保留与旧字段兼容

- 准备：在 `overlay.json` 同时写入有效的 `league_root`、`voice_enabled` 和 `voice_name`。
- 操作：切换语音开关并触发一次英雄联盟路径保存。
- 预期：三项配置互不覆盖；`voice_enabled` 控制内置离线语音，旧 `voice_name`
  字段保留但不会被语音适配器读取。
- 实际：
- 结果：通过 / 不通过

### 17. 本地气泡语音摘要与四轮推荐

- 准备：保持语音开启，依次进入等待、推荐、识别失败和推荐数据不足状态。
- 操作：听取各状态播报，并对照气泡内容。
- 操作：完成四轮海克斯选择，其中两轮连续产生相同的推荐摘要。
- 预期：播报简短口语摘要，不逐字朗读长气泡；重复状态不会反复播报。
- 预期：每轮推荐都播报，包括连续相同摘要；推荐装备及其他已有业务摘要也播报。
- 预期：推荐和关键错误为高优先级消息。
- 实际：
- 结果：通过 / 不通过

### 18. OCR 延迟播报

- 准备：保持语音开启，分别触发一次不超过 2 秒和一次严格超过 2 秒的 OCR。
- 操作：听取 OCR 期间的播报。
- 预期：不超过 2 秒时不播报进度。
- 预期：严格超过 2 秒时仅播报一次“正在识别海克斯，请稍候”。
- 实际：
- 结果：通过 / 不通过

### 19. 优先级、节流与静音

- 准备：连续触发低优先级陪伴消息，并在一条低优先级消息播放时触发推荐或关键错误。
- 操作：观察 60 秒内的低优先级消息，再在播放期间关闭语音。
- 预期：低优先级播报间隔至少 60 秒；高优先级消息会打断低优先级消息。
- 预期：关闭语音会停止当前播放并清除低优先级队列，重新开启后不补播失效消息。
- 实际：
- 结果：通过 / 不通过

### 20. 语音失败隔离与退出清理

- 准备：先正常触发内置离线语音；再分别临时移走模型和禁用音频设备。
- 操作：在两种故障情况下完成启动、OCR、推荐、拖动和退出；退出后检查日志和任务管理器。
- 预期：日志明确记录模型或音频设备错误；语音停用，主链路保持可用。
- 预期：退出不挂起，任务管理器中没有遗留 Python 离线语音 worker。
- 实际：
- 结果：通过 / 不通过

### 21. 死亡等级 OCR 门控

- 准备：能记录死亡瞬间等级和当前 `confirmed_count`。
- 操作：覆盖 3 级首轮，以及 7、11、15 级边界上下的死亡；同时覆盖等级缺失、
  连续的 `isDead=true` 帧和已显示真实三选一。
- 预期：低于 7 级的死亡不 OCR；7–10 级只在已选少于 2 次时 OCR；
  11–14 级只在已选少于 3 次时 OCR；15 级及以上只在已选少于 4 次时 OCR。
- 预期：等级缺失时本次死亡不 OCR；重复死亡帧不重算资格；真实三选一仍可完成 OCR。
- 实际：
- 结果：通过 / 不通过

### 22. 启动挥手门控

- 准备：未进入游戏，启动 GameBuddy。
- 操作：在启动 3 秒挥手期间模拟底层瞬时识别错误，并观察挥手结束后的等待状态。
- 预期：挥手期间不显示失败姿态、OCR 错误气泡或错误语音；结束后回到等待姿态。
- 实际：
- 结果：通过 / 不通过

## 2026-09-22 离线语音与 OCR 实机记录

| 用例 | Windows / 音频设备 | 操作 | 预期 | 实际 | 结果 | `overlay.log` |
| --- | --- | --- | --- | --- | --- | --- |
| 1. 断网首次启动 | 未执行 | 断网且未安装 sherpa-onnx 时启动 | 仅用本地资源启动 | 当前主机为 macOS | 未执行 | 未执行 |
| 2. 本地 wheel 安装 | 未执行 | 检查 pip 命令与安装来源 | 带 `--no-index`，全部来自 wheelhouse | 当前主机为 macOS | 未执行 | 未执行 |
| 3. 启动与选英雄播报 | 未执行 | 双击启动并进入选英雄 | 应用启动并听到选英雄语音 | 当前主机为 macOS | 未执行 | 未执行 |
| 4. 四轮推荐与业务消息 | 未执行 | 四轮选择，包含连续相同摘要和装备推荐 | 每轮及其他业务消息均播报 | 当前主机为 macOS | 未执行 | 未执行 |
| 5. 3/7/11/15 级死亡门控 | 未执行 | 按边界和 `confirmed_count` 触发死亡 | OCR 严格符合资格表 | 当前主机为 macOS | 未执行 | 未执行 |
| 6. 等级缺失与重复死亡帧 | 未执行 | 发送缺等级死亡和重复 dead 帧 | 不开放或重算死亡 OCR | 当前主机为 macOS | 未执行 | 未执行 |
| 7. 启动挥手隔离 | 未执行 | 未入局时启动并观察 3 秒 | 无失败姿态、气泡或语音 | 当前主机为 macOS | 未执行 | 未执行 |
| 8. 语音开关与打断 | 未执行 | 播放中关闭，再开启后触发新消息 | 当前停止，新消息可播报 | 当前主机为 macOS | 未执行 | 未执行 |
| 9. 模型/音频设备降级 | 未执行 | 移走模型；另测禁用音频设备 | 日志明确，推荐主链路继续 | 当前主机为 macOS | 未执行 | 未执行 |
| 10. 进程检查与清理 | 未执行 | 检查播报期间并退出应用 | 无语音 PowerShell worker，退出后无 Python worker | 当前主机为 macOS | 未执行 | 未执行 |

## 验收结论

- 多显示器实机：待验收
- Windows x64 断网首次安装：未执行
- 真实 MeloTTS 中文播放：未执行
- 无语音 PowerShell worker / 无残留 Python worker：未执行
- 总体结果：等待 Windows x64 实机验收
- 未通过项编号：
- 补充日志或截图：
